service RouteGuide{
    
    // RPC methods go in here.
    // There are 4 types: Simple, req stream, res stream, and bi stream

    rcp SayHello(Text) returns (stream Message) {}

}

message Message{
    string Text= "Hey";
}